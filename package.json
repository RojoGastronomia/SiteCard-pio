{
  "name": "cardapio-micro",
  "version": "1.0.0",
  "description": "Sistema de cardápio e eventos",
  "main": "server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "build": "node -e \"require('fs-extra').copySync('frontend/public', 'dist', {overwrite: true})\"",
    "preview": "serve dist"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "mongoose": "^8.1.0",
    "serve": "^14.2.1",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
